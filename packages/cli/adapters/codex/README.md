Copy this dir's contents into your project's `.codex/` dir (`cp adapters/codex/* .codex/`).
Trust/allow `.codex/guard.sh` when Codex prompts — it only pipes prompts/tool calls to `datacloak guard`.
Set `DATACLOAK_VAULT` per session to share one vault file across prompts and tool calls.
